Put your Porcupine keyword files (`*.ppn`) and the Polish acoustic model
`porcupine_params_pl.pv` here. They are tied to your Picovoice account and are
not part of this repository. See `settings.example.py` for the file names the
code expects.
